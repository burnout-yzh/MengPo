# 当前进度

- [x] 当前阶段人工审计项已清空

## 本次会话改动（精简）

- 审计收尾：`scanner/store_flow/atomic_store/database/schema/retrieval*/embeddings/reranker/freshness/dedup*/consistency` 与 `scripts/*` 全链路巡检并落修。
- 安全与稳定：修复 `scan_memory_dir` 根路径 bug；`apply_merge_append` 增加越界校验与并发锁；`Database.transaction` 增加互斥；`atomic_store` 增加 embedding 类型/UTF-8/JSON 前置校验。
- 配置一致性：向量维度改为配置驱动（`embedding.dim`），并在 embedding/rerank 路径增加维度一致性检查（主流程启用，测试兼容）。
- 可观测性：retrieval telemetry/duoshe 读取改 best-effort；`retrieval.log_s1_stats` 开关接入；MCP tool 文案改为“何时用+怎么用”精简版。
- 调试开关：`bowl.yaml` 新增 `storage.debug_log_to_file` 与 `storage.debug_log_path`，服务端与 `inject_memory.py` 落地文件 debug 日志。
- 注入进度日志：`inject_memory.py` 增加“开始嵌入/每200条耗时/完成总量与总耗时”调试输出。
- 脚本规范：`inject_sample/inject_memory/bridge/s1_probe` 统一包方式运行（移除 `sys.path` 注入），并补异常友好处理。
- 结果验证：`python -m pytest -q` 通过（`168 passed, 1 skipped, 7 subtests passed`）。

## 生产联调发现问题（2026-05-17）

- [ ] **MCP 启动即崩溃（导入顺序）**：`D:\MengPo\memory_mcp\server.py` 在 `_db_lock = threading.Lock()` 之前未导入 `threading`，导致 `NameError`，MCP 无法启动。
- [ ] **opencode MCP 字段名陷阱**：`mcp.local` 的环境变量键名应为 `environment`（不是 `env`）。误用后 `MINIMAX_API_KEY` 不会注入，MiniMax MCP 报 `MINIMAX_API_KEY environment variable is required`。
- [ ] **注入入口过宽导致污染**：当前 `bowl.yaml` 的 `injection.memory_dir` 若指向 `D:\BaiduNetdiskWorkspace\pawpaw_memory` 根目录，会递归注入所有 `*.md`，包括 `skills/*.md`、appendix 等非记忆正文。
- [ ] **已验证污染现象**：检索结果已出现 `source_file: skills\\guidance\\SKILL.md`，说明技能文档进入向量库，造成召回噪声与记忆语义偏移。
- [ ] **扫描卡顿/失控风险**：若 `memory/` 平级目录被第三方工具写入大量 Markdown（临时文件、说明文档、备份），全量注入耗时会显著上升，严重时可能“看似卡死”。
- [ ] **配置建议（待在 E 盘修复）**：将注入根目录收束为 `...\\pawpaw_memory\\memory`，避免扫描平级杂项；必要时增加目录白名单或排除规则（如 `skills/**`, `tool_result*/**`, `backup/**`）。
- [ ] **数据安全风险（高优先级）**：历史记忆中存在明文敏感凭据片段（如 token）。即使主流程可用，也会被检索召回，需尽快清洗源文件并重建索引。

## 部署备注

- [ ] 本次 E -> D 同步计划使用 `robocopy /MIR`（镜像模式），执行前确认是否需要排除运行态文件（如 `*.db`、`*.log`、`.mengpo_initialized`）。
